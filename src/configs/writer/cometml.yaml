_target_: src.logger.CometMLWriter
project_name: "SoundStream"
workspace: null
run_name: "testing"
mode: "online"
loss_names: ["loss", "g_rec", "g_adv", "g_feat", "g_commit", "d_loss"] # which losses to log (useful for multi-loss tasks, s.a. GANs)
log_checkpoints: False # set to True if you want to log save_dir with checkpoints to W&B
id_length: 32 # length of the run_id
